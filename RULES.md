# 妙笔 - 检查规则说明

## 已实现的检查功能

### 1. 标点符号检查 ✅

#### 成对标点不成对
- 检查：双引号""、单引号''、圆括号（）、方括号【】、书名号《》等
- 示例：`"这是一个测试` → 提示缺少闭合引号

#### 标点连用/重复
- 检查：连续的逗号、句号等
- 示例：`这是测试。。` → 建议改为 `这是测试。`

#### 标点前后空格
- 检查：中文标点前不应有空格
- 示例：`这是测试 ，看看` → 建议改为 `这是测试，看看`
- 检查：中文标点后接英文应有空格
- 示例：`你好，world` → 建议改为 `你好， world`

#### 全半角标点混用
- 检查：中英文标点混用
- 示例：`他说："hello"` → 提示混用
- 检查：中文中使用半角标点
- 示例：`这是"测试"` → 建议改为 `这是"测试"`

#### 标点位置错误
- 检查：句首不应有标点
- 检查：成对标点内无内容
- 示例：`""` → 提示标点内无内容

---

### 2. 格式检查 ✅

#### 多余空格
- 检查：连续多个空格
- 示例：`这是  测试` → 建议改为 `这是 测试`
- 检查：中文字之间的空格
- 示例：`这 是 测试` → 建议改为 `这是测试`
- 检查：数字与汉字之间缺少空格
- 示例：`100个` → 建议改为 `100 个`

#### 全半角检查
- 检查：全角数字和字母
- 示例：`１２３` → 建议改为 `123`
- 示例：`ＡＢＣ` → 建议改为 `ABC`

#### 千分位检查
- 检查：4位及以上数字应使用千分空格
- 示例：`10000` → 建议改为 `10 000`（不间断空格）

#### 单位间隙检查
- 检查：数值与单位之间应有不间断空格
- 示例：`100kg` → 建议改为 `100 kg`
- 示例：`25℃` → 建议改为 `25 ℃`

---

### 3. 数字格式检查 ✅

#### 罗马数字
- 检查：小写罗马数字应改为大写
- 示例：`第iv章` → 建议改为 `第IV章`

#### 阿拉伯数字规范
- 检查：全角数字应改为半角
- 示例：`１２３` → 建议改为 `123`
- 检查：字母l误用为数字1
- 示例：`l00` → 建议改为 `100`

#### 概数书写
- 检查：概数不应使用顿号
- 示例：`10、几个` → 建议改为 `10几个`
- 检查：范围应使用波浪号
- 示例：`10、20` → 建议改为 `10～20`

#### 时间日期格式
- 检查：日期格式规范
- 示例：`2024.3.31` → 建议改为 `2024年3月31日`
- 检查：中文文本中的时间格式
- 示例：`12:30:00` → 建议改为 `12时30分00秒`

---

### 4. 繁简体和异体字检查 ✅

#### 繁体字检查
- 检查：识别常见繁体字（500+）
- 示例：`國家` → 建议改为 `国家`
- 示例：`學習` → 建议改为 `学习`

#### 异体字检查
- 检查：识别常见异体字（300+）
- 示例：`囯` → 建议改为 `国`
- 示例：`峯` → 建议改为 `峰`

---

### 5. 易错词检查 ✅

#### "的地得"检查
- 规则：动词后表程度用"得"
- 示例：`跑的很快` → 建议改为 `跑得很快`
- 规则：形容词修饰名词用"的"
- 示例：`美丽地花` → 建议改为 `美丽的花`
- 规则：副词修饰动词用"地"
- 示例：`慢慢的走` → 建议改为 `慢慢地走`

#### 常见同音字混淆
- **在/再**：`在见` → `再见`、`再哪里` → `在哪里`
- **做/作**：`做为` → `作为`、`作饭` → `做饭`
- **像/象**：`好象` → `好像`
- **已/以**：`以经` → `已经`、`已前` → `以前`
- **其它/其他**：`其它` → `其他`
- **账/帐**：`帐号` → `账号`、`账篷` → `帐篷`
- **必须/必需**：`必需要` → `必须要`、`必须品` → `必需品`
- **反应/反映**：`反应情况` → `反映情况`
- **制定/制订**：`制订法律` → `制定法律`

#### 重复词检查
- 检查：连续重复的字（排除叠词）
- 示例：`的的` → 建议改为 `的`
- 检查：重复的词语
- 示例：`这个这个` → 建议改为 `这个`

#### 字序颠倒
- 检查：常见颠倒词
- 示例：`明证` → 建议改为 `证明`
- 示例：`要重` → 建议改为 `重要`

---

### 6. 单位符号检查 ✅

#### 错误的单位写法
- 检查：汉字单位应改为符号
- 示例：`100米` → 建议改为 `100 m`
- 示例：`25摄氏度` → 建议改为 `25 ℃`
- 示例：`5千克` → 建议改为 `5 kg`

#### 单位大小写
- 检查：单位符号大小写规范
- 示例：`100M` → 建议改为 `100 m`（米）
- 示例：`5v` → 建议改为 `5 V`（伏特）
- 示例：`10w` → 建议改为 `10 W`（瓦特）

#### 单位间隙
- 检查：数值与单位之间应有不间断空格
- 示例：`100kg` → 建议改为 `100 kg`

---

## 严格程度说明

### basic（基础）
- ✅ 易错词检查
- ✅ 繁简体/异体字检查

### standard（标准，默认）
- ✅ 基础检查
- ✅ 格式检查
- ✅ 数字格式检查

### strict（严格）
- ✅ 标准检查
- ✅ 标点符号检查（全套）
- ✅ 单位符号检查

---

## 测试示例

### 示例 1：综合测试
```
输入：他跑的很快，大概有100、200米的样子。这是測試。
错误：
1. "跑的很快" → "跑得很快"（的地得错误）
2. "100、200" → "100～200"（概数格式）
3. "測試" → "测试"（繁体字）
```

### 示例 2：标点测试
```
输入：这是一个测试 ，看看效果。。
错误：
1. "测试 ，" → "测试，"（标点前空格）
2. "。。" → "。"（标点重复）
```

### 示例 3：格式测试
```
输入：这  是  测试，１２３个。
错误：
1. "这  是" → "这 是"（多余空格）
2. "是  测试" → "是 测试"（多余空格）
3. "１２３" → "123"（全角数字）
```

### 示例 4：单位测试
```
输入：温度是25摄氏度，重量100千克。
错误：
1. "25摄氏度" → "25 ℃"（单位符号）
2. "100千克" → "100 kg"（单位符号）
```

---

## 技术实现

### 检查器架构
```
CheckService
├── PunctuationChecker  # 标点检查
├── FormatChecker       # 格式检查
├── NumberChecker       # 数字检查
├── CharacterChecker    # 繁简体检查
├── TypoChecker         # 易错词检查
└── UnitChecker         # 单位检查
```

### 检查流程
1. 根据严格程度选择检查器
2. 各检查器独立执行
3. 收集所有错误
4. 去重并按位置排序
5. 返回结果

---

## 下一步计划

### 短期（1-2周）
- [ ] 添加更多易错词规则
- [ ] 完善"的地得"判断逻辑
- [ ] 添加词库管理功能
- [ ] 优化检查性能

### 中期（1-2月）
- [ ] 接入分词工具（jieba）
- [ ] 添加词性分析
- [ ] 实现语义检查
- [ ] 添加自定义规则

### 长期（3月+）
- [ ] 接入大语言模型
- [ ] 实现智能改写
- [ ] 添加上下文分析
- [ ] 支持专业领域检查
